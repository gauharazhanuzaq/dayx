import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  template: `
    <h1>Login</h1>
    <p>Auth to be added.</p>
  `
})
export class LoginComponent { }
